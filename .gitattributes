# 行尾规范化
#
# 为什么必须有这个文件：shell 脚本和 systemd 单元带 CRLF 会直接失效——
#   · *.sh  → Linux 上 `bad interpreter: /bin/bash^M`
#   · *.service → systemd 解析异常
# 本项目在 Windows 上开发、在 Ubuntu 上执行，这个坑必踩。
# 显式声明后，即使某台 Windows 机器开了 core.autocrlf=true，
# 这些文件在仓库里和 Linux 检出时都保持 LF。

* text=auto eol=lf

# 强制 LF（会被 Linux 直接执行的）
*.sh      text eol=lf
*.service text eol=lf
*.py      text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf

# 二进制，绝不转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.safetensors binary
*.gguf binary
*.ckpt binary
*.npz  binary

# 密钥与环境
.env
*.key

# 运行产物
output/
logs/
*.log
__pycache__/
*.pyc
venv/

# 模型与大数据（不入库，靠制品库分发）
*.safetensors
*.gguf
*.ckpt
wheels/
comfyui-src.tar.gz

# ── 例外：benchmarks/raw/ 是证据链，必须入库 ──────────────────
# README 承诺"每个性能数字都有原始数据文件可查"，
# 上面的 logs/ 规则本会误伤原始基准数据，这里显式放行。
!benchmarks/raw/
!benchmarks/raw/**
